<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link href="{{ asset('assets/css/style.css') }}" rel="stylesheet"/>
            <link href="{{ asset('assets/css/bootstrap.min.css') }}" rel="stylesheet"/>
            <link href="{{ asset('assets/fontawesome/fontawesome.all.min.css') }}" rel="stylesheet"/>
        {% endblock %}
    </head>
    <body class="custom-font-color">
        <nav class="navbar navbar-expand-lg pb-3 pt-3 mb-5">
            <div class="container-fluid">
                <img src="{{ asset('assets/images/logo.png') }}" alt="Logo" id="logo" class="me-4">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <div class="menu">
                        <ul class="navbar-nav" id="navbar">
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle fs-5 custom-font-color" id="navbarDropdown" role="button"
                                data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Liens
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <a class="dropdown-item color-orange" href="{{ path('app_lien_index') }}">Voir tous les liens</a>
                                    <a class="dropdown-item custom-font-color" href="{{path('app_lien_new')}}">Créer un nouveau lien</a>
                                </div>
                            </li>
                            {% if app.user %}
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle fs-5 custom-font-color" id="navbarDropdown" role="button"
                                data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Mon compte
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <a class="dropdown-item custom-font-color" href="{{path('app_logout')}}">Se déconnecter</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item custom-font-color" href="{{path('app_account')}}">Gérer mon compte</a>
                                </div>
                            </li>
                            {% else %}
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle fs-5 custom-font-color" href="" id="navbarDropdown" role="button"
                                data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Non connecté
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <a class="dropdown-item custom-font-color" href="{{path('app_login')}}">Se connecter</a>
                                    <a class="dropdown-item custom-font-color" href="{{path('app_register')}}">S'inscrire</a>
                                </div>
                            </li>
                            {% endif %}
                        </ul>
                    </div>
                    <div class="menu">
                        <p class="fs-1 nav-title">Watson by DVS</p>
                    </div>
                    <div class="menu">
                        <form class="d-flex form-search" action="" method="post">
						<input class="form-control me-sm-2" type="search" name="Keywords" placeholder="Rechercher">
						<button class="btn my-2 my-sm-0 ms-0" type="submit" id="btn-submit"><i class="fa-search-location"></i></button>
					</form>
                    </div>
                </div>
            </div>
        </nav>
        <div class="container-fluid">
        {% if app.user %}
        {% else %}
            <h2>Vous n'êtes pas connecté</h2>
        {% endif %}
        {% block body %}{% endblock %}
        </div>
        {% block javascripts %}
            <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </body>
</html>