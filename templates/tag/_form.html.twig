{% if app.user %}
    {{ form_start(form, {'attr': {'class': 'needs-validation'}}) }}
        <div class="mb-3">
            {{ form_row(form.tag_name, {'label': 'Nom', 'attr': {'class': 'form-control', 'placeholder': 'Saisissez une nom...'}}) }}
        </div>
        <div class="d-flex justify-content-center mt-4">
            <button class="btn btn-block btn-signup me-3">{{ button_label|default('Save') }}</button>
            <a href="{{ path('app_tag_index') }}" class="btn btn-outline btn-login">Retourner à la liste</a>
        </div>
    {{ form_end(form) }}
{% endif %}