{% extends 'base.html.twig' %}

{% block title %}Lien index{% endblock %}

{% block body %}

    <h1> Liste des liens </h1>

        {% for lien in pagination %}
        
            <div class="miniature col-6 col-md-4 col-lg-3" >
                <a href="{{ path('app_lien_show', {'id': lien.id}) }}"><center><img class='miniature' src="{{ lien.lienImage }}"></center>
                <div class='container'> <p> {{lien.lienTitre}} </p></div></a>
            </div>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
            
        {% endfor %}
        </tbody>
    </table>
    {{ knp_pagination_render(pagination) }}

    <a href="{{ path('app_lien_new') }}">Create new</a>
{% endblock %}
